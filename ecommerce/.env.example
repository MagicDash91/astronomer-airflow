# Copy to .env (gitignored) and fill in. NEVER commit real secrets.

# --- Snowflake ---
SNOWFLAKE_ACCOUNT=JNJUCIN-BE85650
SNOWFLAKE_USER=MAGICDASH
SNOWFLAKE_PASSWORD=            # from a secrets manager, not committed
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=E_COMMERCE
SNOWFLAKE_SCHEMA=PUBLIC
# Prefer the least-privilege role once provisioned (see terraform/snowflake.tf.example)
SNOWFLAKE_ROLE=ACCOUNTADMIN

# --- AWS (S3 landing + Terraform) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-southeast-1
S3_LANDING_BUCKET=magicdash-olist-ecommerce-raw-data

# --- Optional: WhatsApp notification (Meta Cloud API) ---
# Leave unset to log notifications only.
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_RECIPIENT=
